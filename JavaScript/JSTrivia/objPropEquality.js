const a = {
  firstName: "Bill",
};

const b = {
  firstName: "Bill",
};

console.log(a.firstName === b.firstName);
// What logs to the console above??

// Line 9 should log 'true' because the code evaluates to 'Bill' === 'Bill', which will result in 'true'.
