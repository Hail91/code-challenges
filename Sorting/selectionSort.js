// Selection Sort here....
const selectionSort = () => {
  // Need to find research on how this works since I cannot currently remember!
  return null;
};

console.log(selectionSort([2, 5, 7, 1, 4, 3, 9, 6, 10]));
